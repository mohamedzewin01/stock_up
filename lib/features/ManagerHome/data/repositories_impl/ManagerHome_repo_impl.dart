import 'package:injectable/injectable.dart';
import '../../domain/repositories/ManagerHome_repository.dart';

@Injectable(as: ManagerHomeRepository)
class ManagerHomeRepositoryImpl implements ManagerHomeRepository {
  // implementation
}
