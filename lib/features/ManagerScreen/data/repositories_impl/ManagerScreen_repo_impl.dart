import 'package:injectable/injectable.dart';
import '../../domain/repositories/ManagerScreen_repository.dart';

@Injectable(as: ManagerScreenRepository)
class ManagerScreenRepositoryImpl implements ManagerScreenRepository {
  // implementation
}
